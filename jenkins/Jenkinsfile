pipeline {
    agent {
        docker {
            image 'docker.desaysv.com/ici2/tools/maven-edoc:20240112'
            args '-v /root/.m2:/root/.m2'
        }
    }
    environment {
        CI = 'true'
    }
    stages {
        stage("Clone code from github") {
            steps {
                script {
                    git  credentialsId: 'wangzhaokai', url: 'git@github.com:wangzhaokai11/simple-java-maven-app.git'
                }
            }
        }
        stage("Maven Build") {
            steps {
                script {
                    sh "mvn package -DskipTests=true"
                    // sh "cp /var/jenkins_home/workspace/demo1_master/target/my-app-1.0-SNAPSHOT.jar "
                }
            }
        }
        stage("Publish to Nexus Repository Manager") {
            steps {
                script {
                    echo '执行edoc上传'
                    sh "./pipeline-edoc.sh"
                }
            }
        }
    }
}
